interface:
  display_name: "TanStack Router"
  short_description: "Type-safe routing guidance for navigation, URL state, data loading, authentication, SSR, and framework adapters."
  default_prompt: "Use $tanstack-router to implement this TanStack Router task with the installed framework and package versions."
